.onAppear {
    fetchEmbeddingsJSON(for: "7062240121") { result in
        switch result {
        case .success(let json):
            print(json)         // ← pretty-printed compact JSON
        case .failure(let err):
            print("API error:", err)
        }
    }
}
